
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow write access for any authenticated user during development.
    // In production, you should restrict this to admin users.
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
