
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for any authenticated user.
    // This is for development purposes only.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
